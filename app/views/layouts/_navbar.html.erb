<nav class="navbar navbar-expand-lg navbar-dark bg-success">
    <div class="container">
        <!-- <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button> -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                <%= button_to "Home", root_path, method: :get, :class => 'btn btn-light' %>
                </li>
                <% if logged_in? %>
                    <li class="nav_item">
                        <%= button_to "New Listing", new_listing_path, method: :get, :class => 'btn btn-success'%>
                    </li>
                    <li class="nav_item">
                        <%= button_to "My Listings", '/my_listings', method: :get, :class => 'btn btn-success'%>
                    </li>
                    <li class="nav_item">
                        <%= button_to "My Bids", '/my_bids', method: :get, :class => 'btn btn-success'%>
                    </li>
                    <li class="nav_item">
                        <%= button_to "Bids On My Listings", '/bids_on_my_listings', method: :get, :class => 'btn btn-success'%>
                    </li>
                    <li class="nav_item">
                        <%= button_to "View Contractors", skills_path, method: :get, :class => 'btn btn-success'%>
                    </li>
                    <li class="nav_item">
                        <%= button_to "Messages", conversations_path, method: :get, :class => 'btn btn-success'%>
                    </li>
                    <li class="nav_item">
                        <%= button_to "Logout", logout_path, :class => 'btn btn-dark'%>
                    </li>
                <% else %>
                    <li class="nav-item">
                        <%= button_to "Login", login_path, method: :get, :class => 'btn btn-success' %>
                    </li>
                    <li class="nav-item">
                        <%= button_to "Sign Up", signup_path, method: :get, :class => 'btn btn-success' %>
                    </li>
                    <li class="nav-item">
                        <%= button_to 'Sign in with GitHub', '/auth/github', method: :post, :class => 'btn btn-dark'%>
                    </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>